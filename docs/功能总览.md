# 四足轮腿机器人 — 功能总览 v1.0

> 本文件是机器人**当前所有功能的总索引**，后续新增/修改功能直接在本文件更新，并同步在文末"更新日志"追加一条。
> 命令细节以 [协议/蓝牙通信协议.md](协议/蓝牙通信协议.md)（v6.4）为准；标定与余量以 [硬件/3DIK标定数据.md](硬件/3DIK标定数据.md) 为准。

---

## 一、硬件配置

| 模块 | 配置 |
|------|------|
| 主控 | STM32H743ZITx @480MHz |
| 舵机 | 12×270° 数字舵机（三块舵机板），50Hz PWM，5ms 批量更新 |
| 轮式电机 | 4×MG513 + TB6612×2，PWM 驱动（占空比 0~100%）；轮径 **108mm**（2026-08-20 更换，旧 65mm） |
| 编码器 | 4×GMR 编码器，EXTI 双沿计数（满速输出轴 ≈360 RPM，50% 占空比 ≈180 RPM） |
| IMU | WT9011G4K 九轴，USART2 115200bps，上电时序双保险（ErrorCallback 自愈 + 看门狗） |
| 蓝牙 | HC-05 SPP 115200bps，TX 走 DMA（带 D-Cache 刷新） |

---

## 二、功能分类总表

| 类别 | 功能 | 命令 |
|------|------|------|
| 姿态 | 狗高站姿 280mm | `G` / `H` |
| 姿态 | 前顶高站姿（前腿反折膝）280mm | `K` |
| 姿态 | 后顶高站姿（后腿反折膝）280mm | `P` |
| 姿态 | 前顶低趴 191mm（四轮驱动，大腿≈水平+小腿垂直视觉姿态） | `L` |
| 姿态 | 后顶低趴 240mm（四轮驱动） | `M` |
| 姿态 | **收纳（贴地趴）** 72mm 慢速缓降（断电放置） | `C` |
| 姿态 | **规划式站起**（顶升+收腿到前顶低趴 191，免翻膝约 3.5s） | `U` |
| 姿态 | **坐姿**（前腿竖直+后腿反折深蹲，狗坐造型） | `O` |
| 云台 | **云台舵机**（180° ×2：水平 pan / 俯仰 tilt，上电不动，居中 90/90） | `G:水平:俯仰` |
| 姿态 | 翻膝机动（姿态切换自动执行，约 3s） | 自动，回显 `FLIP` |
| 步态 | 连续 Trot（步幅 60/抬腿 25/1.2s） | `T` |
| 步态 | 越障模式（四轮 30% + 对角抬腿 70mm 前伸步） | `W` |
| 步态 | 转圈动作（顺/逆时针） | `E` / `E:0` |
| 步态 | 单步调试（相位 0/90/180/270） | `A` |
| 轮式 | 一键前进/后退/停（30%） | `R` / `B` / `S` |
| 轮式 | 电机单控 | `M0:50:1` |
| 轮式 | **摇杆遥控**（差速+斜坡+倾斜平衡） | `V:速度:转向` |
| 轮式 | **方向按键**（固定倾斜全程保持，S 才回平） | `D:方向[:速度]` |
| 坡度 | 上坡自适应（轮式模式下身体前压贴坡） | `Z` 标定开关，`Q:i:v` 调参 |
| 传感 | IMU 姿态上报（10Hz，520 帧） | `I` 开关 |
| 传感 | 编码器转速上报（10Hz，520 帧） | `J` 开关 |
| 修正 | 前倾修正（足端前后移） | `X:毫米` |
| 校准 | 舵机校准（重装舵机臂用） | `1~12` / `Y` / `N` |

---

## 三、姿态系统（五姿态 + 翻膝）

### 3.1 五姿态

| 姿态 | 站高 | 前腿膝 | 后腿膝 | 用途 |
|------|------|--------|--------|------|
| G/H 狗高 | 280 | 正常 | 正常 | 行走、越障 |
| K 前顶高 | 280 | 反折 | 正常 | 展示/特殊 |
| P 后顶高 | 280 | 正常 | 反折 | 展示/特殊 |
| L 前顶低趴 | 220 | 反折 | 正常 | 四轮驱动 |
| M 后顶低趴 | 220 | 正常 | 反折 | 四轮驱动（重心集中） |

- 低趴姿态足端展开（L 四足 ±129.5 对称、M 前 +45/后 -120），膝高(几何) L 180mm、M 134/112mm，实际离地再 +54mm（108mm 轮径），支撑中心在身下
- 上电默认：狗姿态 280mm（`G` 即可站立）

### 3.2 翻膝机动

- 前后腿膝分支（正常/反折）在"直腿点"（足端伸到 L1+L2）连续
- 切换需要翻膝的组合时自动执行：足端伸到直腿点 → 翻转分支 → 收回，约 3s
- K↔L、M↔P 同分支直接升降，无翻膝
- 全程膝离地 ≥110mm，回显 `FLIP`

---

## 四、步态系统

| 功能 | 说明 |
|------|------|
| `T` 纯 Trot | 对角抬腿行走 + **四轮 10% 助走**，步幅 60mm / 抬腿 25mm / 周期 1.2s / 支撑比 0.6；退出行走（G 等）自动停轮 |
| `T:-1` 倒走 | 负步幅反向 trot + 四轮 10% 反向助走 |
| `W` 越障 | 四轮 30% 前进 + 高站姿 280 + 对角抬腿 70mm（摆动相前伸 50mm，支撑相回收推身体前进），可跨 ~50mm 障碍（108mm 大轮） |
| `E`/`E:0` 转圈 | trot + 15mm 重心横移，原地顺/逆时针旋转 |
| `A` 单步 | 相位冻结，每次 +1/4 周期，调试用 |

---

## 五、轮式系统

### 5.1 一键与单控

- `R` 四轮 30% 前进 / `B` 后退 / `S` 急停
- `M电机号:速度[:方向]` 单电机控制（2026-08-20 重接线后四路同极性，无方向补偿）

### 5.2 摇杆遥控（APP 第四页）

```
命令: V:速度:转向   范围 −50~+50, V 值 = 占空比% (1:1)
      速度负=后退, 转向负=左转/正=右转
差速: 左轮 = 速度+转向, 右轮 = 速度−转向 (限幅±100)
斜坡: 0.5s 渐变到目标占空比 (起步不猛)
回中: V:0:0 → 渐停 + 回平 (急停用 S)
回显: 无 (高频流, APP 不要等应答)
```

### 5.3 倾斜平衡（V 自动附带）

| 场景 | 动作 | 幅度 |
|------|------|------|
| 起步加速 | 平台低头（前腿 z 缩短/后腿 z 伸长） | ∝(命令−实际占空比)，上限 15mm ≈4.5° |
| 刹车减速 | 平台抬头 | 反向 |
| 左/右转 | 向弯内压弯（左右 z 差动） | ∝转向量，上限 10mm ≈2.7° |
| 到速 | 编码器平台期判定（300ms 窗口增量 <2 RPM）→ 回平 | 0.3s 渐变 |
| 兜底 | 斜坡完成自动回平（编码器无信号也可用） | — |

---

## 六、坡度自适应（轮式爬坡）

- `Z`：当前姿态采样 2s 标定零偏 + 开启；再按关闭（每姿态单独存零偏）
- 生效条件：轮式模式（`R` 或 `V` 驱动中）
- 行为：上坡（pitch 长期均值 < −5°）→ 足端整体后移（身体前压）→ 车身贴坡不后仰
- 参数（`Q:参数号:值` 在线调）：

| # | 参数 | 默认 | 含义 |
|---|------|------|------|
| 0 | lpf_alpha | 0.002 | 坡度低通（约 2.5s） |
| 1 | up_thr | 5° | 上坡判定阈值 |
| 2 | hyst | 2° | 退出迟滞 |
| 3 | gain | 3.5 mm/° | 前压力度 |
| 4 | shift_max | 40 mm | 后移上限 |

---

## 七、传感数据上报

| 流 | 开关 | 频率 | 帧格式 |
|----|------|------|--------|
| IMU 角度 | `I` | 10Hz | `R,x,P,y,Y,z,520`（×10 定点，0.1°） |
| 编码器转速 | `J` | 10Hz | `E,e0,e1,e2,e3,520`（RPM） |

- 帧尾 520 校验；上报与命令回显交错，上位机按前缀分发
- IMU 符号：前倾 P 正、后倾 P 负（上坡 = P 负）

---

## 八、关键参数（当前值）

| 参数 | 值 | 位置 |
|------|-----|------|
| 腿长 L1/L2/髋偏置 | 130/180/10 mm | leg_ik.h |
| 机身 BL/BW | 380/180 mm | leg_ik.h |
| 站高 低/高 | 220/280 mm | walk_gait.h |
| 前倾修正 X | 15 mm | walk_gait.c |
| 轮径 | 108 mm（旧 65mm 已更换） | 硬件 |
| 车速 | 50% 占空比 ≈180 RPM ≈ **1.0 m/s**（108mm 轮；旧轮 0.6） | 换算 |
| 转圈横移量 | 15 mm（定死） | walk_gait.c |
| 越障抬腿 | 70 mm | main.c |
| 倾斜上限 | 低头 15mm / 压弯 10mm | drive_ctrl.c |
| 转速换算 | V 值 = 占空比%，满速 50 ≈180 RPM | drive_ctrl.c |
| 坡度参数 | 0.002 / 5° / 2° / 3.5 / 40 | attitude_control.c |

---

## 九、常用操作流程

```
上电 → READY
  ├─ G → T         纯行走
  ├─ G → W         越障
  ├─ G → E/E:0     转圈
  ├─ L/M → R/B/S   四轮驱动（低趴）
  ├─ L/M → Z → R   坡度自适应爬坡
  ├─ V:30:0        摇杆遥控（倾斜自动）
  ├─ I / J         传感数据显示
  └─ X:毫米        前倾修正
```

---

## 十、已知限制与待办

- [ ] 低趴 240 时勿发 T（会钳位）；行走前先 `G`/`H`
- [ ] 坡度功能尚未在真实斜坡实测（斜坡待搭建）
- [ ] 横移走 F 已暂停（代码保留）
- [ ] 镇定层（站立自稳/行走镇定）暂缓
- [ ] 编码器功能仅用于显示与倾斜平台期判定
- [ ] 舵机校准命令当前启用，正式版改回 `#if 0`

---

## 更新日志

| 日期 | 内容 |
|------|------|
| 2026-08-19 | v1.0 初版：五姿态、步态、轮式+摇杆遥控+倾斜平衡、坡度、传感上报、校准、修正 |
| 2026-08-19 | 新增 D 方向按键（固定倾斜全程保持、S 才回平）；修正压弯方向 bug（左转=左边低）；V 映射改 1:1（V 值=占空比%） |
| 2026-08-19 | D 按键默认速度 30；弧线调紧（转向分量 spd/2→3spd/5）；倾斜加大（低头/抬头 10mm、压弯 10mm） |
| 2026-08-19 | D 按键切换动作改为"先回平再倾斜"两段式（约 0.2s+0.2s），不再直接横摆 |
| 2026-08-19 | 新增 C 收纳命令（150mm 最深低趴，断电放置安全落地；G 一键站起） |
| 2026-08-19 | C 升级为 72mm 贴地趴（慢速缓降 2~3s，断电落差仅 ~32mm）；新增 U 规划式站起（阶段A收腿+顶升 3.5s + 阶段B翻膝升 280） |
| 2026-08-19 | U 上电后直接可用（g_booted 门控，13 处命令清除点）；C 收纳保持单段贴地趴（收小腿方案因断电落差权衡撤销） |
| 2026-08-20 | 更换轮毂 65→108mm（半径+21.5mm）：膝高几何值不变、实际离地 +21.5mm；C 收纳断电落差 32→~53mm（着地轮子锚定车身）；同占空比车速约 +67%（50% ≈1.0 m/s）；越障上限提高 |
| 2026-08-20 | 低趴高度 220→240：108mm 大轮负载下 FR 站起堵转，抬高 20mm 缩短升站行程、足端内收降低力矩（余量 20°/12°→32°/24°） |
| 2026-08-20 | 新增单腿足端零位修正 foot_x_corr[4]：RR 落地点偏前（小腿零位偏差，实测），RR 补 −20mm（−30 实测偏后回退）；后续机械重标 RR 零位后移除 |
| 2026-08-20 | T 前进行走新增四轮 10% 助走（E 转圈不助走）；退出行走模式（G 等姿态命令）自动停轮，急停用 S |
| 2026-08-21 | L 前顶低趴改视觉姿态（191mm、四足 ±129.5 对称、大腿≈水平+小腿垂直）；U 站起改单段（顶升+收腿直接站到 L，免翻膝约 3.5s） |
| 2026-08-21 | 新增坐姿 O（前腿竖直伸直+后腿反折深蹲，狗坐造型，最小余量 13.3°）+ 倒着走 T:-1（负步幅+轮反向助走） |
| 2026-08-22 | 新增云台双舵机（TIM8 CH3/CH4：PC8/PC9，50Hz），蓝牙 G:水平:俯仰 控制，上电默认 135/135 |
| 2026-08-20 | W 越障单腿依次抬方案实测效果差，改回对角交替抬 + 摆动相前伸 50mm（抬起→前伸→落下→回收，支撑相推身体前进），轮子持续前推 |
| 2026-08-20 | 升站慢速平滑：站高上升方向（低趴→高姿态）平滑速率 0.12→0.04（约 1.5s 渐变），大轮负载下舵机跟得上；下降与翻膝保持原速 |
